@model List<OMInsurance.WebApps.Models.FundResponsePackageModel>
<div class="container-wide">
    @for (int i = 0; i < Model.Count(); i++ )
    {
        ViewBag.IsFirstResponseInPackage = (i == 0);
        @Html.Partial("_FundResponsePackageModel", Model[i])
    }
</div>
